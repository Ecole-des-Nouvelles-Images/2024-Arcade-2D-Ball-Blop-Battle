using UnityEngine;

namespace Hugo.Prototype.Scripts.Player
{
    public class PlayerSoundsPart : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
